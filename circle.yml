machine:
  pre:
    - mkdir ~/assets/
#    - curl -k -L -o ~/assets/planet-150202.osm.bz2 ftp://ftp.spline.de/pub/openstreetmap/planet/2015/planet-150202.osm.bz2
dependencies:
  pre:
    - sudo apt-get update
    - curl -C -k -L -o ~/assets/cov-analysis-linux64-7.6.0.tar.gz http://sd-55475.dedibox.fr/cov-analysis-linux64-7.6.0.tar.gz
#    - sudo apt-get upgrade
    - sudo apt-get install cmake 
#    - sudo apt-get install zlib1g-dev
    - sudo apt-get install libpng12-dev 
    - sudo apt-get install libgtk2.0-dev 
    - sudo apt-get install librsvg2-bin 
    - sudo apt-get install g++
    - ls 
    - ls navit
    - bash ~/navit/build.sh
  cache_directories:
    - "~/assets/"
