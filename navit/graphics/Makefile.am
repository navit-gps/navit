SUBDIRS=null
if GRAPHICS_GTK_DRAWING_AREA
  SUBDIRS+=gtk_drawing_area
endif
if GRAPHICS_OPENGL
  SUBDIRS+=opengl
endif
if GRAPHICS_QT_QPAINTER
  SUBDIRS+=qt_qpainter
endif
if USE_GRAPHICS_SDL
  SUBDIRS+=sdl
endif
if GRAPHICS_WIN32
  SUBDIRS+=win32
endif
if GRAPHICS_GD
  SUBDIRS+=gd
endif
