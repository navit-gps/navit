SUBDIRS=
if SPEECH_ANDROID
  SUBDIRS += android
endif
if SPEECH_CMDLINE
  SUBDIRS += cmdline
endif
if SPEECH_DBUS
  SUBDIRS += dbus
endif
if SPEECH_ESPEAK
  SUBDIRS += espeak
endif
if SPEECH_SPEECH_DISPATCHER
  SUBDIRS += speech_dispatcher
endif
