SUBDIRS=null
if GRAPHICS_GTK_DRAWING_AREA
  SUBDIRS+=gtk_drawing_area
endif
if DIRECTFB
  SUBDIRS+=directfb
endif
if GRAPHICS_OPENGL
  SUBDIRS+=opengl
if GUI_CLUTTER
  SUBDIRS+=cogl
endif
endif
if USE_GRAPHICS_QT_QPAINTER
  SUBDIRS+=qt_qpainter
endif
if USE_GRAPHICS_SDL
  SUBDIRS+=sdl
endif
if GRAPHICS_WIN32
  SUBDIRS+=win32
endif
