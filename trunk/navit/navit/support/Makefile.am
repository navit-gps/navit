SUBDIRS=
if SUPPORT_EZXML
  SUBDIRS+=ezxml
endif
if SUPPORT_GLIB
  SUBDIRS+=glib
endif
if SUPPORT_WORDEXP
  SUBDIRS+=wordexp
endif
if SUPPORT_WIN32
  SUBDIRS+=win32
endif
if SUPPORT_ZLIB
  SUBDIRS+=zlib
endif
