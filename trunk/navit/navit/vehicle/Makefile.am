SUBDIRS=
if VEHICLE_MAEMO
  SUBDIRS += maemo
endif
if VEHICLE_ANDROID
  SUBDIRS += android
endif
if VEHICLE_IPHONE
  SUBDIRS += iphone
endif
if VEHICLE_DEMO
  SUBDIRS += demo
endif
if VEHICLE_FILE
  SUBDIRS += file
endif
if VEHICLE_GPSD
  SUBDIRS += gpsd
endif
if VEHICLE_GPSD_DBUS
  SUBDIRS += gpsd_dbus
endif
if VEHICLE_GYPSY
  SUBDIRS += gypsy
endif
if VEHICLE_NULL
  SUBDIRS += null
endif
if VEHICLE_WINCE
  SUBDIRS += wince
endif
